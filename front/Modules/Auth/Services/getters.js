export const isLogged = (state) => {
  return state.isLogged
}
export const role = (state) => state.data.role
export const user = (state) => state.user
export const errors = (state) => state.errors
export const isAuthLoaded = (state) => state.isLoaded
