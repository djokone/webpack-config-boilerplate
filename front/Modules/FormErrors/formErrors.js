class FormErrors {
  construct () {
    this.errors = {}
  }

  saveErrors (errors) {
    this.errors = errors
  }

  getErrors () {
    this.getErrors()
  }
}

/* eslint-disable no-new */
export default new FormErrors()
